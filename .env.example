# ---- Client-safe (exposed to the browser) ----
NEXT_PUBLIC_SUPABASE_URL=https://tocqmqooilxrynijvyvr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=            # public anon key — safe in client, protected by RLS

# ---- Server-ONLY (set in Vercel + Supabase; NEVER commit real values) ----
SUPABASE_SERVICE_ROLE_KEY=                # bypasses RLS — server/Edge Functions only

# ---- Payments (added later, when the accounts exist) ----
# DARAJA_CONSUMER_KEY=
# DARAJA_CONSUMER_SECRET=
# DARAJA_PASSKEY=
# DARAJA_SHORTCODE=
# FLUTTERWAVE_SECRET_KEY=
# FLUTTERWAVE_WEBHOOK_HASH=
